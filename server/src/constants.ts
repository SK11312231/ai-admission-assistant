/**
 * The placeholder value that ships in .env.example.
 * Both the server startup check and the setup script compare against this string
 * so that if the placeholder is ever changed in .env.example, only one place
 * needs to be updated.
 */
export const ENV_PLACEHOLDER = 'your_key_here';
